apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "event-system.fullname" . }}-test-connection"
  labels:
    {{- include "event-system.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "event-system.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
